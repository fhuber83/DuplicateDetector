<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DuplicateDetector_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DuplicateDetector_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" VerticalAlignment="Stretch">
        <CommandBar VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Green">
            <AppBarButton>Click me</AppBarButton>
        </CommandBar>
        <ListView Margin="10" Background="White" VerticalAlignment="Stretch">
            <ListView.Items>
                <ListViewItem Content="Test1"/>
                <ListViewItem Content="Test2"/>
                <ListViewItem Content="Test3"/>
            </ListView.Items>
        </ListView>
        <CommandBar VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Red">
            <AppBarButton>Click me instead!</AppBarButton>
        </CommandBar>
    </StackPanel>
</Window>
